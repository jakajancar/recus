# CLAUDE.md

Before starting, always:

- **Read rec.us API docs** in `docs/api.md`
- **Check logged-in accounts**: `uv run recus accounts`

When working:

- **Prefer using `recus get` to query the API**: `uv run recus get --auth /v1/...`
